# 개발 규칙

1. **TDD 원칙 준수**: 모든 개발 작업은 반드시 TDD(Test-Driven Development) 원칙에 따라 진행한다.
   - 테스트 코드를 먼저 작성하고, 이후 구현 코드를 작성한다.
   - Red → Green → Refactor 사이클을 반드시 지킨다.

2. **E2E 테스트 코드 작성**: E2E 테스트 환경이 갖춰진 프로젝트라면, 반드시 E2E 테스트 코드를 작성한다.
   - 정상 흐름(Happy Path)뿐만 아니라, 네트워크 오류, 타임아웃, 권한 없음, 잘못된 입력, 빈 상태, 동시 요청 등 온갖 예외 상황도 빠짐없이 커버한다.
   - 경계값, 엣지 케이스, 예상치 못한 사용자 행동까지 고려해 테스트를 작성한다.

3. **사이드 이펙트 검사**: 개발 작업이 끝나면 `code-review-side-effects` 에이전트를 실행해 사이드 이펙트 여부를 반드시 검사한다.

4. **보안 취약점 검사**: 개발 작업이 끝나면 `security-auditor` 에이전트를 실행해 보안 취약점 여부를 반드시 검사한다.
